import React from 'react'

const Details = (props) => {
    console.log(props.location.detailProps);
    
    
    return (
        <div>
            <h2>Test</h2>
        </div>
    )
}

export default Details
